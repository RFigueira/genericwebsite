import React, { Component } from 'react';

export default class Preco extends Component {
    constructor(props) {
        super(props);
    };

    render() {
        return(
            <h1>Pagina de preços</h1>
        );
    }
}